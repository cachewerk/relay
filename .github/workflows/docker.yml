name: Test Docker

on: [push, workflow_dispatch]

jobs:

  builds:

    name: Docker
    runs-on: ubuntu-latest
    timeout-minutes: 5

    strategy:
      fail-fast: false
      matrix:
        version:
          - v0.4.0
          - dev
        build:
          - alpine
          - amzn2
          - centos7
          - centos8
          - debian10
          - debian11-apt
          - ubuntu18-apt
          - ubuntu20
          - ubuntu22-apt
          - litespeed

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - uses: satackey/action-docker-layer-caching@v0.0.11
        continue-on-error: true
        with:
          key: ${{ matrix.build }}-${{ matrix.version }}-{hash}
          restore-keys: ${{ matrix.build }}-${{ matrix.version }}

      - name: Build Docker container
        run: |
          docker build --pull \
            --tag ${{ matrix.build }}-${{ matrix.version }} \
            --file docker/${{ matrix.build }}.Dockerfile .

      - name: Run tests
        env:
          SHELL: "${{ matrix.build == 'alpine' && 'sh' || 'bash' }}"
        run: |
          docker run ${{ matrix.build }}-${{ matrix.version }} $SHELL -c "php --ri relay"
